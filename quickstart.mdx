---
title: Quickstart
description: Get up and running with TailStack in under 5 minutes
---

# Quickstart

Get your TailStack project running in minutes. This guide will help you scaffold, install, and start developing with any of the three TailStack templates.

<Info>
  **First time here?** This quickstart uses the **Core monorepo** template. For React-only or Node-only setups, see the [Installation](/installation) guide.
</Info>

## Get started in 3 steps

<Steps>
  <Step title="Scaffold the project">
    Use `degit` to create a new project from the Core monorepo template:

    ```bash
    npx degit GitCoder052023/TailStack/packages/core my-project
    cd my-project
    ```

    This downloads the complete TailStack Core architecture with a clean Git history.
  </Step>

  <Step title="Install dependencies">
    TailStack uses **pnpm** for fast, efficient dependency management.

    <Tabs>
      <Tab title="Linux / macOS">
        Run the smart install script with load monitoring:

        ```bash
        bash scripts/install.sh
        ```

        Or use pnpm directly:

        ```bash
        pnpm install
        ```
      </Tab>

      <Tab title="Windows (PowerShell)">
        Run the smart install script with load monitoring:

        ```powershell
        .\scripts\install.ps1
        ```

        Or use pnpm directly:

        ```powershell
        pnpm install
        ```
      </Tab>
    </Tabs>

    <Tip>
      The smart install scripts automatically monitor CPU and RAM usage, suspending installations if load exceeds 90% and resuming when it drops below 75%.
    </Tip>
  </Step>

  <Step title="Start development">
    Start both the frontend and backend servers concurrently:

    ```bash
    pnpm dev
    ```

    Your application is now running:
    - **Frontend**: [http://localhost:5173](http://localhost:5173)
    - **Backend API**: [http://localhost:5000](http://localhost:5000)
    - **Weather API Demo**: [http://localhost:5173/weather](http://localhost:5173/weather)
  </Step>
</Steps>

## What you just created

Your TailStack Core project includes:

<CardGroup cols={2}>
  <Card title="Frontend" icon="react">
    - React 19 with TypeScript
    - Vite 6 for lightning-fast HMR
    - Tailwind CSS 4 with Shadcn UI
    - React Router for navigation
    - Weather app demo
  </Card>

  <Card title="Backend" icon="server">
    - Express 5 with TypeScript
    - Node.js clustering (multi-core)
    - CORS and security middleware
    - Weather API endpoints
    - Environment configuration
  </Card>

  <Card title="Monorepo Tools" icon="layer-group">
    - PNPM workspaces
    - Husky Git hooks
    - Commitlint for conventional commits
    - Gitleaks security scanning
    - Cross-platform automation scripts
  </Card>

  <Card title="AI Agent Skills" icon="wand-magic-sparkles">
    - Vercel React Best Practices
    - Node.js Backend Patterns
    - Tailwind v4 + Shadcn UI
    - Pre-configured for Claude, Cursor, OpenCode
  </Card>
</CardGroup>

## Explore the demo

TailStack includes a fully functional **Weather App** to showcase the architecture:

1. Navigate to [http://localhost:5173/weather](http://localhost:5173/weather)
2. Search for a city or use your current location
3. Explore the code:
   - Frontend: `source/frontend/src/pages/weather.tsx`
   - API Routes: `source/Server/src/routes/weather.routes.ts`
   - Controller: `source/Server/src/controller/weather.controller.ts`

## Next steps

<CardGroup cols={2}>
  <Card title="Project Structure" icon="folder-tree" href="/architecture/project-structure">
    Understand the directory organization and file structure
  </Card>

  <Card title="Development Workflow" icon="code" href="/development/workflow">
    Learn the development workflow, Git hooks, and best practices
  </Card>

  <Card title="Building with Core" icon="hammer" href="/guides/building-with-core">
    Build your first feature with the monorepo architecture
  </Card>

  <Card title="Deployment" icon="rocket" href="/guides/deployment">
    Deploy your TailStack application to production
  </Card>
</CardGroup>

## Alternative templates

Not building a full-stack application? Choose a different template:

<Tabs>
  <Tab title="React Only">
    For frontend-only applications:

    ```bash
    npx degit GitCoder052023/TailStack/packages/react my-react-app
    cd my-react-app
    pnpm install
    pnpm dev
    ```

    Perfect for dashboards, landing pages, and client-side apps.

    [Learn more about the React template →](/architecture/react-template)
  </Tab>

  <Tab title="Node Only">
    For backend-only APIs and services:

    ```bash
    npx degit GitCoder052023/TailStack/packages/node my-api
    cd my-api
    pnpm install
    pnpm dev
    ```

    Ideal for REST APIs, microservices, and backend utilities.

    [Learn more about the Node template →](/architecture/node-template)
  </Tab>
</Tabs>

## Troubleshooting

<AccordionGroup>
  <Accordion title="pnpm command not found">
    Install pnpm globally:

    ```bash
    npm install -g pnpm
    ```

    Or use Corepack (Node.js 16.13+):

    ```bash
    corepack enable
    corepack prepare pnpm@latest --activate
    ```
  </Accordion>

  <Accordion title="Port already in use">
    If port 5173 or 5000 is already in use, you can change the ports:

    **Frontend** - Edit `source/frontend/vite.config.ts`:
    ```typescript
    server: {
      port: 3000  // Change to your preferred port
    }
    ```

    **Backend** - Create a `.env` file in `source/Server/`:
    ```bash
    PORT=4000  # Change to your preferred port
    ```
  </Accordion>

  <Accordion title="Node version mismatch">
    TailStack requires Node.js 20.x or higher. Check your version:

    ```bash
    node --version
    ```

    If using nvm:

    ```bash
    nvm use
    ```

    Or install the correct version:

    ```bash
    nvm install 20
    ```
  </Accordion>
</AccordionGroup>

## Get help

<CardGroup cols={2}>
  <Card title="GitHub Issues" icon="github" href="https://github.com/GitCoder052023/TailStack/issues">
    Report bugs or request features
  </Card>

  <Card title="Installation Guide" icon="book" href="/installation">
    Detailed installation instructions
  </Card>
</CardGroup>
